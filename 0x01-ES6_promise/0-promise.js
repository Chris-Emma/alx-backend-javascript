#!/usr/bin/node

function getResponseFromAPI() {
  return new Promise(() => {
    // API call or asynchronous operation
    // Resolve or reject the promise
  });
}

export default getResponseFromAPI;
